# Portfolio

Single-page static site. `index.html` holds the markup, styles and script; images live in `public/assets/`.

```bash
npm install
npm run dev     # local dev server
npm run build   # -> dist/
```

The 2025 React/Vite version is archived on the `old-2025` branch.
